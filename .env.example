# App Environment: local | production
APP_ENV=local

# WebSocket Server Port
WS_PORT=8080

# Database Configuration (optional — game works without DB)
DB_HOST=localhost
DB_NAME=typing_battle
DB_USER=root
DB_PASS=
